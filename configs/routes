{
  "base": "http://services.runescape.com/m=itemdb_oldschool",
  "endpoints": {
    "catalog": {
			"url": "/api/catalogue/items.json?category=1&alpha={name}&page={page}",
			"keys": [ "alpha", "page" ]
		},
    "detail": {
			"url": "/api/catalogue/detail.json?item={itemid}",
			"keys": [ "item" ]
		},
    "graph": {
			"url": "/api/graph/{itemid}.json",
			"keys": [ "item" ]
		}
  },
  "placeholders": {
    "item": "{itemid}",	
    "page": "{page}",
    "alpha": "{name}"
  }
}
